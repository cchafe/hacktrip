guiDemo.pro builds chugin, qt5 only because of chuck
qmake guiDemo.pro
make clean
make
cd ck/
chuck --chugin-path:../chugin/ --srate:48000 serverPolyphony.ck 

gui.pro builds simple gui app in app/, either qt5 or qt6
qmake gui.pro
...or...
qmake6 gui.pro
make clean
make
/////////////// do this once in the terminal since libs are not system installed
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/cc/hacktrip/guiDemo/rtaudio/
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/cc/hacktrip/guiDemo/hapitrip/
/////////////// (or just build and run in creator which keeps track of libs it creates)
app/app


